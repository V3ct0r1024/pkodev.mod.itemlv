#pragma once

namespace pkodev
{
	namespace address
	{
        // Game.exe 0 (1222073761)
        namespace GAME_13X_0
        {
            // void CItemCommand::Render(int x, int y)
            const unsigned int CItemCommand__Render = 0x00488F00; 

            // void CItemCommand::SaleRender(int x, int y, int nWidth, int nHeight)
            const unsigned int CItemCommand__SaleRender = 0x00488BD0;

            // inline CItemRecord* GetItemRecordInfo( int nTypeID )
            const unsigned int GetItemRecordInfo = 0x0040CE90;

            // SItemForge& SItemForge::Convert(DWORD v, int nItemID)
            const unsigned int SItemForge__Convert = 0x0048A590;

            // CGuiFont
            const unsigned int CGuiFont = 0x0066A730;

            // void CGuiFont::Render( const char* str, int x, int y, DWORD color,float size )
            const unsigned int CGuiFont__Render = 0x00423FB0;

            // CMPFont
            const unsigned int CMPFont = 0x0066A740;

            // SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
            const unsigned int CMPFont__GetTextSize = 0x00600710;

            // class UIRender
            const unsigned int UIRender = 0x00654000;

            // void UIRender::FillFrame(int left, int top, int right, int bottom, DWORD color=0x90000000)
            const unsigned int UIRender__FillFrame = 0x0049F070;
        }

        // Game.exe 1 (1243412597)
        namespace GAME_13X_1
        {
            // void CItemCommand::Render(int x, int y)
            const unsigned int CItemCommand__Render = 0x00488FC0;

            // void CItemCommand::SaleRender(int x, int y, int nWidth, int nHeight)
            const unsigned int CItemCommand__SaleRender = 0x00488C90;

            // inline CItemRecord* GetItemRecordInfo( int nTypeID )
            const unsigned int GetItemRecordInfo = 0x0040CE90;

            // SItemForge& SItemForge::Convert(DWORD v, int nItemID)
            const unsigned int SItemForge__Convert = 0x0048A650;

            // CGuiFont
            const unsigned int CGuiFont = 0x0066A730;

            // void CGuiFont::Render( const char* str, int x, int y, DWORD color,float size )
            const unsigned int CGuiFont__Render = 0x0048A650;

            // CMPFont
            const unsigned int CMPFont = 0x0066A740;

            // SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
            const unsigned int CMPFont__GetTextSize = 0x00600710;

            // class UIRender
            const unsigned int UIRender = 0x00654000;

            // void UIRender::FillFrame(int left, int top, int right, int bottom, DWORD color=0x90000000)
            const unsigned int UIRender__FillFrame = 0x0049F180;
        }

        // Game.exe 2 (1252912474)
        namespace GAME_13X_2
        {
            // void CItemCommand::Render(int x, int y)
            const unsigned int CItemCommand__Render = 0x00489060;

            // void CItemCommand::SaleRender(int x, int y, int nWidth, int nHeight)
            const unsigned int CItemCommand__SaleRender = 0x00488D30;

            // inline CItemRecord* GetItemRecordInfo( int nTypeID )
            const unsigned int GetItemRecordInfo = 0x0040CE90;

            // SItemForge& SItemForge::Convert(DWORD v, int nItemID)
            const unsigned int SItemForge__Convert = 0x0048A6F0;

            // CGuiFont
            const unsigned int CGuiFont = 0x0066A730;

            // void CGuiFont::Render( const char* str, int x, int y, DWORD color,float size )
            const unsigned int CGuiFont__Render = 0x00423FB0;

            // CMPFont
            const unsigned int CMPFont = 0x0066A740;

            // SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
            const unsigned int CMPFont__GetTextSize = 0x00600710;

            // class UIRender
            const unsigned int UIRender = 0x00654000;

            // void UIRender::FillFrame(int left, int top, int right, int bottom, DWORD color=0x90000000)
            const unsigned int UIRender__FillFrame = 0x0049F220;
        }

        // Game.exe 3 (1244511158)
        namespace GAME_13X_3
        {
            // void CItemCommand::Render(int x, int y)
            const unsigned int CItemCommand__Render = 0x00489050;

            // void CItemCommand::SaleRender(int x, int y, int nWidth, int nHeight)
            const unsigned int CItemCommand__SaleRender = 0x00488D20; 

            // inline CItemRecord* GetItemRecordInfo( int nTypeID )
            const unsigned int GetItemRecordInfo = 0x0040CE90;

            // SItemForge& SItemForge::Convert(DWORD v, int nItemID)
            const unsigned int SItemForge__Convert = 0x0048A6E0;

            // CGuiFont
            const unsigned int CGuiFont = 0x0066A730;

            // void CGuiFont::Render( const char* str, int x, int y, DWORD color,float size )
            const unsigned int CGuiFont__Render = 0x00423FB0;

            // CMPFont
            const unsigned int CMPFont = 0x0066A740;

            // SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
            const unsigned int CMPFont__GetTextSize = 0x00600710;

            // class UIRender
            const unsigned int UIRender = 0x00654000;

            // void UIRender::FillFrame(int left, int top, int right, int bottom, DWORD color=0x90000000)
            const unsigned int UIRender__FillFrame = 0x0049F210;
        }

        // Game.exe 4 (1585009030)
        namespace GAME_13X_4
        {
            // void CItemCommand::Render(int x, int y)
            const unsigned int CItemCommand__Render = 0x00489500;// 0x00489060;

            // void CItemCommand::SaleRender(int x, int y, int nWidth, int nHeight)
            const unsigned int CItemCommand__SaleRender = 0x00488EA0;// 0x00488D30;

            // inline CItemRecord* GetItemRecordInfo( int nTypeID )
            const unsigned int GetItemRecordInfo = 0x0040CF90;

            // SItemForge& SItemForge::Convert(DWORD v, int nItemID)
            const unsigned int SItemForge__Convert = 0x0048A8E0;

            // CGuiFont
            const unsigned int CGuiFont = 0x0066A748;

            // void CGuiFont::Render( const char* str, int x, int y, DWORD color,float size )
            const unsigned int CGuiFont__Render = 0x004240E0;

            // CMPFont
            const unsigned int CMPFont = 0x0066A758;

            // SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
            const unsigned int CMPFont__GetTextSize = 0x00600710;

            // class UIRender
            const unsigned int UIRender = 0x00654008;

            // void UIRender::FillFrame(int left, int top, int right, int bottom, DWORD color=0x90000000)
            const unsigned int UIRender__FillFrame = 0x0049F420;
        }

        // Game.exe 5 (1207214236)
        namespace GAME_13X_5
        {
            // void CItemCommand::Render(int x, int y)
            const unsigned int CItemCommand__Render = 0x00488BD0; // 0x00489060;

            // void CItemCommand::SaleRender(int x, int y, int nWidth, int nHeight)
            const unsigned int CItemCommand__SaleRender = 0x004888A0;// 0x00488D30;

            // inline CItemRecord* GetItemRecordInfo( int nTypeID )
            const unsigned int GetItemRecordInfo = 0x0040CE80;

            // SItemForge& SItemForge::Convert(DWORD v, int nItemID)
            const unsigned int SItemForge__Convert = 0x0048A260;

            // CGuiFont
            const unsigned int CGuiFont = 0x0066A738;

            // void CGuiFont::Render( const char* str, int x, int y, DWORD color,float size )
            const unsigned int CGuiFont__Render = 0x00423FA0;

            // CMPFont
            const unsigned int CMPFont = 0x0066A748;

            // SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
            const unsigned int CMPFont__GetTextSize = 0x00600710;

            // class UIRender
            const unsigned int UIRender = 0x00654000;

            // void UIRender::FillFrame(int left, int top, int right, int bottom, DWORD color=0x90000000)
            const unsigned int UIRender__FillFrame = 0x0049EE70;
        }
	}
}